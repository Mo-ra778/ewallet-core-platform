# مشروع نظام محفظة إلكترونية (E-Wallet System)
## تطبيق مبدأ Client-Server Architecture مع نظام إشعارات وتحقق OTP
### مقترح مشروع تخرج - مادة الشبكات

---

## 1. المقدمة

### 1.1 فكرة المشروع
تطبيق عملي لمبدأ **Client-Server Architecture** من خلال بناء نظام محفظة إلكترونية متكامل. المشروع يوضح كيفية تواصل عدة عملاء (Clients) مع خادم مركزي (Server) عبر بروتوكولات الاتصال القياسية.

### 1.2 المبدأ المطبق
**Client-Server Model**: نموذج توزيعي يقسم المهام بين:
- **العملاء (Clients)**:
  - Flutter Mobile App (المستخدم)
  - Agent Web (الوكيل — مبني بلارافيل Blade)
  - Admin Dashboard (الأدمن — مبني بلارافيل Blade)
- **الخادم (Server)**: Laravel Backend API

### 1.3 لماذا المحفظة الإلكترونية؟
لأنها تتطلب:
- اتصال مستمر بين عدة عملاء والخادم
- معالجة Request/Response بشكل آمن
- إدارة Sessions وتوثيق الهوية عبر الشبكة
- تبادل بيانات حساسة (معلومات مالية) يتطلب حماية
- عدة أدوار (مستخدم + وكيل + أدمن) يتواصلون مع نفس الخادم
- نظام إشعارات يربط بين الأطراف
- **نظام OTP (One-Time Password)** للتحقق من العمليات الحساسة

---

## 2. أهداف المشروع (من منظور الشبكات)

### الأهداف الرئيسية:
1. **فهم Client-Server Architecture** وتطبيقها عملياً مع عدة عملاء
2. **استخدام بروتوكول HTTP/HTTPS** كطبقة تطبيق (Application Layer)
3. **فهم Request/Response Cycle** بين العميل والخادم
4. **تطبيق TCP/IP** كبروتوكولات نقل وشبكة
5. **إدارة Ports** (Port 3000 للـ API)
6. **توثيق الهوية عبر الشبكة** باستخدام JWT (JSON Web Tokens)
7. **معالجة Network Errors** وTimeouts
8. **نظام إشعارات** — إرسال واستلام الإشعارات بين الأطراف
9. **نظام OTP** — التحقق بخطوتين للعمليات الحساسة

### الأهداف الثانوية:
- فهم RESTful APIs كـ Interface بين العميل والخادم
- تطبيق JSON كـ Data Format للتبادل
- فهم كيف يتعامل عدة عملاء (Multi-Client) مع خادم واحد
- فصل الصلاحيات (Role-Based Access Control)
- فهم آليات الأمان متعددة الطبقات (JWT + OTP)

---

## 3. وصف النظام والأدوار

### 3.1 المستخدم العادي (Flutter Mobile App)
- **يسجل حساب جديد**: يدخل كل البيانات → الحساب يُنشأ بحالة "**pending**" (معلق)
- **ينتظر موافقة الأدمن**: ما يقدر يسوي أي عملية لين يوافق الأدمن
- **يدخل بعد الموافقة**: يشوف رصيده ويستخدم النظام
- **يحول رصيد لمستخدم ثاني**
- **يستلم OTP** على التطبيق لما الوكيل يسوي سحب
- **يستلم إشعارات**: إيداع جديد، سحب، تحويل واصل، إشعار من الأدمن، **"تمت موافقة حسابك"**

### 3.2 الوكيل (Agent Web — Laravel Blade)
- يدخل على `/agent/login` بحسابه
- يستقبل طلبات الإيداع/السحب من المستخدمين
- **الإيداع**: يدخل رقم المستخدم + المبلغ + العملة → يؤكد مباشرة
- **السحب**:
  - يدخل رقم المستخدم + المبلغ + العملة
  - يضغط "طلب سحب"
  - **النظام يرسل OTP للمستخدم**
  - المستخدم يعطي الوكيل الكود
  - الوكيل يدخل الكود ويضغط "تأكيد السحب"
  - النظام يتحقق من الكود وينفذ السحب
- يشوف رصيده كوكيل
- **يستلم إشعارات**: من الأدمن (تنبيهات، تحذيرات، إلخ)
- **كل واجهات الوكيل مبنية بـ Laravel Blade** (مو HTML منفصل)

### 3.3 الأدمن (Admin Dashboard)
- **يشوف إحصائيات النظام كامل** (مستخدمين، وكلاء، معاملات)
- **يدير طلبات التسجيل**: يشوف كل الحسابات اللي تنتظر الموافقة (pending) ويقرر: **موافقة** أو **رفض**
- **يدير المستخدمين** (عرض، تعليق/تفعيل الحساب، إضافة رصيد، خصم رصيد)
- **يدير الوكلاء** (عرض، تعليق/تفعيل الحساب، إضافة وكيل)
- **يغذي حساب مستخدم** (إيداع مباشر للمستخدم)
- **يسحب من حساب مستخدم** (سحب مباشر من المستخدم)
- **يشوف سجل المعاملات** مع فلترة وبحث
- **يدخل على تفاصيل أي مستخدم** ويشوف:
  - كل بياناته الشخصية
  - **كل عملياته المالية مع تفاصيل "من" و"إلى"**
- **يدخل على تفاصيل أي عملية** ويشوف بياناتها كاملة
- **يرسل إشعارات**: يقدر يرسل إشعار لعميل محدد أو وكيل محدد

### 3.4 الخادم (Backend API)
- يستقبل الطلبات من التطبيق والوكيل والأدمن
- يعالج المصادقة (JWT) لكل دور
- ينفذ العمليات المالية
- يتحدث مع قاعدة البيانات
- **يدير نظام الإشعارات** (إنشاء، إرسال، تخزين، عرض)
- **يدير نظام OTP** (إنشاء، إرسال، التحقق، انتهاء الصلاحية)

---

## 4. هيكل الشبكة (Network Architecture)

### 4.1 الطبقات المستخدمة (OSI Model)

| الطبقة | البروتوكول/التقنية | الوظيفة |
|--------|-------------------|---------|
| **Application (7)** | HTTP/1.1, HTTPS | نقل البيانات بين التطبيقات |
| **Presentation (6)** | JSON, JWT | تنسيق البيانات والتشفير |
| **Session (5)** | JWT Sessions | إدارة الجلسات |
| **Transport (4)** | TCP | نقل موثوق مع تسلسل |
| **Network (3)** | IP | توجيه الحزم بين الأجهزة |
| **Data Link (2)** | WiFi / Ethernet | الاتصال الفيزيائي |
| **Physical (1)** | 4G/5G, WiFi, Ethernet | الوسائط المادية |

### 4.2 تدفق البيانات (Data Flow)

```
[Flutter App] ──HTTP Request──> [Laravel Server] ──SQL Query──> [PostgreSQL]
[Agent Web]   ──HTTP Request──> [Laravel Server] ──SQL Query──> [PostgreSQL]
[Admin Dash]  ──HTTP Request──> [Laravel Server] ──SQL Query──> [PostgreSQL]
     ^                                                              |
     └────────────── JSON Response <────────────────────────────────┘
```

### 4.3 الـ Ports المستخدمة

| الخدمة | Port | الوصف |
|--------|------|-------|
| Laravel API | 8000 (أو أي Port) | استقبال طلبات HTTP من العميل والوكيل والأدمن |
| PostgreSQL | 5432 | قاعدة البيانات (TCP/IP) |
| Redis | 6379 | تخزين مؤقت للـ OTP (Cache) |

---

## 5. تقنيات الاتصال (Communication Technologies)

### 5.1 HTTP Methods (REST API)

| Method | Endpoint | الوظيفة | الدور |
|--------|----------|---------|-------|
| POST | /api/auth/register | تسجيل مستخدم جديد | Flutter |
| POST | /api/auth/login | تسجيل دخول مستخدم | Flutter |
| POST | /api/agent/login | تسجيل دخول وكيل | Agent Web |
| POST | /api/admin/login | تسجيل دخول أدمن | Admin |
| GET | /api/wallet/balance | الاستعلام عن الرصيد | Flutter |
| POST | /api/wallet/transfer | تحويل لمستخدم آخر | Flutter |
| POST | /api/agent/deposit | إيداع للمستخدم (وكيل) | Agent Web |
| **POST** | **/api/agent/withdraw/request** | **طلب سحب + إرسال OTP** | **Agent Web** |
| **POST** | **/api/agent/withdraw/verify** | **تأكيد السحب بـ OTP** | **Agent Web** |
| POST | /api/admin/deposit | إيداع للمستخدم (أدمن) | Admin |
| POST | /api/admin/withdraw | سحب للمستخدم (أدمن) | Admin |
| POST | /api/admin/user/suspend | تعليق حساب مستخدم | Admin |
| POST | /api/admin/user/activate | تفعيل حساب مستخدم | Admin |
| POST | /api/admin/agent/suspend | تعليق حساب وكيل | Admin |
| POST | /api/admin/agent/activate | تفعيل حساب وكيل | Admin |
| **GET** | **/api/admin/pending-users** | **قائمة طلبات التسجيل (pending)** | **Admin** |
| **POST** | **/api/admin/user/approve** | **موافقة على تسجيل مستخدم** | **Admin** |
| **POST** | **/api/admin/user/reject** | **رفض تسجيل مستخدم** | **Admin** |
| GET | /api/admin/users | قائمة المستخدمين | Admin |
| GET | /api/admin/agents | قائمة الوكلاء | Admin |
| GET | /api/admin/transactions | سجل المعاملات | Admin |
| GET | /api/admin/user/:id | تفاصيل مستخدم + عملياته | Admin |
| GET | /api/admin/transaction/:id | تفاصيل عملية | Admin |
| POST | /api/admin/notify-user | إرسال إشعار لعميل | Admin |
| POST | /api/admin/notify-agent | إرسال إشعار لوكيل | Admin |
| GET | /api/notifications | جلب إشعاراتي | All |
| POST | /api/notifications/read | تحديد الإشعار كمقروء | All |
| GET | /api/wallet/transactions | سجل المعاملات (مستخدم) | Flutter |

### 5.2 Headers المستخدمة

```
Content-Type: application/json
Authorization: Bearer <JWT_TOKEN>
Accept: application/json
```

### 5.3 Response Format (JSON)

---

## 6. عقد API بين الفريق (API Contract for Remote Collaboration)

> **هذا القسم مخصص للتعاون عن بُعد** — يوضح بالضبط ما يُرسل وما يُستلم بين العميل والخادم.

### 6.1 مبدأ العمل عن بُعد

بما أن الفريق يعمل عن بُعد (remote)، فإن **التواصل الوحيد** بين المبرمج الأول (العميل) وأنت (الخادم) هو **عبر API**.

**المبرمج الأول لا يحتاج أن يعرف كيف يعمل الخادم داخلياً** — يكفي أن يعرف:
- ما هو الـ Endpoint (الرابط)
- ما هو الـ Method (GET/POST)
- ما هي الـ Headers المطلوبة
- ما هو شكل الـ Request Body (البيانات المرسلة)
- ما هو شكل الـ Response (البيانات المستلمة)

**أنت لا تحتاج أن تعرف كيف يعمل التطبيق** — يكفي أن تضمن:
- الـ Endpoint يستقبل البيانات الصحيحة
- الـ Response يرجع بالشكل المتفق عليه
- الأخطاء تُرجع برسالة واضحة

### 6.2 قواعد التعاون

| القاعدة | الشرح |
|---------|-------|
| **لا تغير الـ Endpoint بدون إبلاغ** | إذا غيرت الرابط أو البيانات، قول لالمبرمج الأول فوراً |
| **لا تغير شكل الـ Response بدون إبلاغ** | المبرمج الأول يبني التطبيق على الشكل المتفق عليه |
| **استخدم Postman** | المبرمج الأول يختبر الـ API بالـ Postman قبل ما يربطه بالتطبيق |
| **Swagger Docs** | أنت توثق كل Endpoint في Swagger |
| **اجتماع أسبوعي** | مراجعة ما تم إنجازه وما يحتاج تعديل |

### 6.3 تفاصيل كل Endpoint (JSON Contract)

#### A. تسجيل مستخدم جديد
```
POST /api/auth/register

Request Body:
{
  "full_name": "أحمد محمد",
  "phone": "0501234567",
  "email": "ahmed@email.com",
  "password": "123456"
}

Response Success (201):
{
  "success": true,
  "message": "تم إنشاء الحساب، ينتظر موافقة الإدارة",
  "data": {
    "user_id": "uuid-here",
    "status": "pending"
  }
}

Response Error (400):
{
  "success": false,
  "message": "رقم الجوال مستخدم مسبقاً"
}
```

#### B. تسجيل دخول مستخدم
```
POST /api/auth/login

Request Body:
{
  "phone": "0501234567",
  "password": "123456"
}

Response Success (200):
{
  "success": true,
  "message": "تم تسجيل الدخول",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIs...",
    "user": {
      "id": "uuid",
      "full_name": "أحمد محمد",
      "phone": "0501234567",
      "balance": 0.00,
      "status": "active"
    }
  }
}

Response Error (403) — حساب معلق:
{
  "success": false,
  "message": "حسابك قيد المراجعة، ينتظر موافقة الإدارة"
}

Response Error (403) — حساب مرفوض:
{
  "success": false,
  "message": "تم رفض حسابك، تواصل مع الإدارة"
}
```

#### C. الاستعلام عن الرصيد
```
GET /api/wallet/balance
Headers: Authorization: Bearer <TOKEN>

Response Success (200):
{
  "success": true,
  "data": {
    "balance": 1500.00,
    "currency": "SAR"
  }
}

Response Error (401):
{
  "success": false,
  "message": "Unauthorized"
}
```

#### D. تحويل رصيد
```
POST /api/wallet/transfer
Headers: Authorization: Bearer <TOKEN>

Request Body:
{
  "receiver_phone": "0507654321",
  "amount": 200.00,
  "description": "دفع فاتورة"
}

Response Success (200):
{
  "success": true,
  "message": "تم التحويل بنجاح",
  "data": {
    "transaction_id": "uuid",
    "new_balance": 1300.00
  }
}

Response Error (400):
{
  "success": false,
  "message": "رصيد غير كافٍ"
}
```

#### E. جلب الإشعارات
```
GET /api/notifications
Headers: Authorization: Bearer <TOKEN>

Response Success (200):
{
  "success": true,
  "data": [
    {
      "id": "uuid",
      "title": "تم إيداع رصيد",
      "message": "تم إيداع 500 ريال في حسابك",
      "type": "transaction",
      "is_read": false,
      "created_at": "2026-08-21T10:30:00Z"
    }
  ]
}
```

#### F. تسجيل دخول وكيل
```
POST /api/agent/login

Request Body:
{
  "phone": "0509998888",
  "password": "123456"
}

Response Success (200):
{
  "success": true,
  "message": "تم تسجيل الدخول",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIs...",
    "agent": {
      "id": "uuid",
      "full_name": "محمد الوكيل",
      "phone": "0509998888",
      "balance": 5000.00
    }
  }
}
```

#### G. إيداع كاش (وكيل)
```
POST /api/agent/deposit
Headers: Authorization: Bearer <TOKEN>

Request Body:
{
  "user_phone": "0501234567",
  "amount": 100.00,
  "currency": "SAR"
}

Response Success (200):
{
  "success": true,
  "message": "تم الإيداع بنجاح",
  "data": {
    "transaction_id": "uuid",
    "user_new_balance": 1600.00
  }
}
```

#### H. طلب سحب كاش — الخطوة 1 (إرسال OTP)
```
POST /api/agent/withdraw/request
Headers: Authorization: Bearer <TOKEN>

Request Body:
{
  "user_phone": "0501234567",
  "amount": 100.00,
  "currency": "SAR"
}

Response Success (200):
{
  "success": true,
  "message": "تم إرسال كود التحقق للمستخدم"
}

Response Error (400):
{
  "success": false,
  "message": "رصيد المستخدم غير كافٍ"
}
```

#### I. تأكيد سحب كاش — الخطوة 2 (التحقق من OTP)
```
POST /api/agent/withdraw/verify
Headers: Authorization: Bearer <TOKEN>

Request Body:
{
  "user_phone": "0501234567",
  "amount": 100.00,
  "currency": "SAR",
  "otp_code": "123456"
}

Response Success (200):
{
  "success": true,
  "message": "تم السحب بنجاح",
  "data": {
    "transaction_id": "uuid",
    "user_new_balance": 1400.00
  }
}

Response Error (400):
{
  "success": false,
  "message": "كود التحقق غير صحيح أو منتهي الصلاحية"
}
```

#### J. تسجيل دخول أدمن
```
POST /api/admin/login

Request Body:
{
  "username": "admin",
  "password": "admin123"
}

Response Success (200):
{
  "success": true,
  "message": "تم تسجيل الدخول",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIs...",
    "admin": {
      "id": "uuid",
      "username": "admin",
      "role": "super_admin"
    }
  }
}
```

#### K. جلب طلبات التسجيل المعلقة
```
GET /api/admin/pending-users
Headers: Authorization: Bearer <TOKEN>

Response Success (200):
{
  "success": true,
  "data": [
    {
      "id": "uuid",
      "full_name": "أحمد محمد",
      "phone": "0501234567",
      "email": "ahmed@email.com",
      "status": "pending",
      "created_at": "2026-08-21T10:00:00Z"
    }
  ]
}
```

#### L. موافقة على تسجيل مستخدم
```
POST /api/admin/user/approve
Headers: Authorization: Bearer <TOKEN>

Request Body:
{
  "user_id": "uuid-here"
}

Response Success (200):
{
  "success": true,
  "message": "تمت الموافقة على الحساب"
}
```

#### M. رفض تسجيل مستخدم
```
POST /api/admin/user/reject
Headers: Authorization: Bearer <TOKEN>

Request Body:
{
  "user_id": "uuid-here"
}

Response Success (200):
{
  "success": true,
  "message": "تم رفض الحساب"
}
```

#### N. إرسال إشعار من الأدمن
```
POST /api/admin/notify-user
Headers: Authorization: Bearer <TOKEN>

Request Body:
{
  "recipient_phone": "0501234567",
  "title": "تحديث النظام",
  "message": "سيتم تحديث النظام الليلة"
}

Response Success (200):
{
  "success": true,
  "message": "تم إرسال الإشعار"
}
```

### 6.4 Headers المطلوبة في كل طلب

| Header | القيمة | متى تُستخدم |
|--------|--------|------------|
| `Content-Type` | `application/json` | في كل طلب POST/PUT |
| `Authorization` | `Bearer <JWT_TOKEN>` | في كل طلب محمي (بعد تسجيل الدخول) |
| `Accept` | `application/json` | في كل طلب |

### 6.5 كودات الأخطاء (Status Codes)

| الكود | المعنى | متى يظهر |
|-------|--------|---------|
| `200` | نجاح | العملية تمت بنجاح |
| `201` | تم الإنشاء | تسجيل مستخدم جديد |
| `400` | خطأ في البيانات | بيانات ناقصة أو غير صحيحة |
| `401` | غير مصرح | Token مفقود أو منتهي |
| `403` | ممنوع | حساب معلق أو مرفوض |
| `404` | غير موجود | المستخدم أو العملية غير موجودة |
| `500` | خطأ في السيرفر | مشكلة داخلية |

### 6.6 أدوات التعاون عن بُعد

| الأداة | الاستخدام | من يستخدمها |
|--------|----------|------------|
| **Postman** | اختبار الـ API قبل ربطه بالتطبيق | المبرمج الأول |
| **Swagger** | توثيق الـ API (يراه الجميع) | أنت |
| **GitHub** | مشاركة الكود | الاثنين |
| **WhatsApp/Discord** | التواصل اليومي | الاثنين |

### 6.7 خطوات التكامل (Integration Steps)

**الخطوة 1: المبرمج الثاني يبني الـ Endpoint**
- تبني الـ Route + Controller
- تختبه بالـ Postman
- تكتب توثيقه في Swagger

**الخطوة 2: تبلغ المبرمج الأول**
- ترسل له رابط الـ Endpoint
- ترسل له شكل الـ Request والـ Response
- يرسل له ملف Postman Collection (اختياري)

**الخطوة 3: المبرمج الأول يختبر**
- يفتح Postman
- يرسل طلب للـ Endpoint
- يتأكد من الـ Response

**الخطوة 4: المبرمج الأول يربط بالتطبيق**
- يكتب دالة في `api_service.dart`
- يختبرها في التطبيق

**الخطوة 5: مراجعة**
- اجتماع أسبوعي
- حل أي مشاكل في الربط



```json
{
  "success": true,
  "data": {
    "balance": 1500.00,
    "currency": "SAR"
  },
  "message": "Balance retrieved successfully"
}
```

---

## 7. الأمان على مستوى الشبكة (Network Security)

### 7.1 Authentication (طبقة أولى)
- **JWT (JSON Web Tokens)**: توكن موقع (Signed) يُرسل في كل Request
- **Bearer Token**: يُرسل في Header: `Authorization: Bearer <token>`
- **Token Expiry**: صلاحية محددة (24 ساعة)

### 7.2 OTP Verification (طبقة ثانية — للسحب فقط)
- **OTP**: رمز تحقق مكون من 6 أرقام
- **مدة الصلاحية**: 5 دقائق
- **التخزين**: Redis (مؤقت)
- **الإرسال**: إشعار داخل التطبيق (محاكاة)
- **الاستخدام**: مرة واحدة فقط

### 7.3 Authorization (Role-Based)
- **Middleware يتحقق من الدور**: هل المستخدم عادي؟ وكيل؟ أدمن؟
- **كل Endpoint محمي** بصلاحياته الخاصة
- **مثال**: `/api/admin/*` يقبل فقط أدمن

### 7.4 Data Protection
- **HTTPS**: تشفير الاتصال بين العميل والخادم
- **bcrypt**: تشفير كلمات المرور في قاعدة البيانات
- **Input Validation**: التحقق من البيانات المُرسلة

---

## 8. هيكل قاعدة البيانات

### جدول المستخدمين (users)
| الحقل | النوع | الوصف |
|-------|-------|-------|
| id | UUID | المعرف الفريد |
| full_name | VARCHAR(100) | الاسم الكامل |
| phone | VARCHAR(20) | رقم الجوال |
| email | VARCHAR(100) | البريد الإلكتروني |
| password_hash | VARCHAR(255) | كلمة المرور مشفرة |
| balance | DECIMAL(15,2) | الرصيد الحالي |
| status | ENUM | **pending** / active / suspended / rejected |
| created_at | TIMESTAMP | تاريخ الإنشاء |

### جدول الوكلاء (agents)
| الحقل | النوع | الوصف |
|-------|-------|-------|
| id | UUID | المعرف الفريد |
| full_name | VARCHAR(100) | اسم الوكيل |
| phone | VARCHAR(20) | رقم الجوال |
| password_hash | VARCHAR(255) | كلمة المرور مشفرة |
| balance | DECIMAL(15,2) | رصيد الوكيل |
| status | ENUM | active / suspended |
| created_at | TIMESTAMP | تاريخ الإنشاء |

### جدول الأدمن (admins)
| الحقل | النوع | الوصف |
|-------|-------|-------|
| id | UUID | المعرف الفريد |
| username | VARCHAR(50) | اسم المستخدم |
| password_hash | VARCHAR(255) | كلمة المرور مشفرة |
| role | ENUM | super_admin / admin |
| created_at | TIMESTAMP | تاريخ الإنشاء |

### جدول المعاملات (transactions)
| الحقل | النوع | الوصف |
|-------|-------|-------|
| id | UUID | المعرف الفريد |
| user_id | UUID | المستخدم |
| agent_id | UUID | الوكيل (null إذا الأدمن) |
| admin_id | UUID | الأدمن (null إذا الوكيل) |
| type | ENUM | deposit / withdraw / transfer |
| amount | DECIMAL(15,2) | المبلغ |
| currency | VARCHAR(10) | العملة (SAR / USD / إلخ) |
| status | ENUM | completed / failed / pending |
| description | TEXT | الوصف |
| created_at | TIMESTAMP | تاريخ العملية |

### جدول الإشعارات (notifications)
| الحقل | النوع | الوصف |
|-------|-------|-------|
| id | UUID | المعرف الفريد |
| recipient_id | UUID | المستلم (مستخدم أو وكيل) |
| recipient_type | ENUM | user / agent |
| title | VARCHAR(200) | عنوان الإشعار |
| message | TEXT | نص الإشعار |
| type | ENUM | transaction / alert / message / otp |
| is_read | BOOLEAN | هل تم القراءة؟ (false افتراضياً) |
| created_at | TIMESTAMP | تاريخ الإرسال |

---

## 9. هيكل الملفات (Project Structure)

### Backend (Laravel (PHP)) - المبرمج الثاني
```
backend/
├── config/
│   └── db.js                    # الاتصال بقاعدة البيانات
├── controllers/
│   ├── authController.js        # تسجيل دخول (مستخدم + وكيل + أدمن)
│   ├── walletController.js      # تحويل + استعلام
│   ├── agentController.js       # إيداع/سحب كاش (وكيل) + OTP
│   ├── adminController.js       # إحصائيات + إدارة + إيداع/سحب + تعليق/تفعيل
│   └── notificationController.js # إرسال/استلام/عرض الإشعارات
├── routes/
│   ├── authRoutes.js            # مسارات المصادقة
│   ├── walletRoutes.js          # مسارات المحفظة
│   ├── agentRoutes.js           # مسارات الوكيل (مع OTP)
│   ├── adminRoutes.js           # مسارات الأدمن
│   └── notificationRoutes.js    # مسارات الإشعارات
├── middleware/
│   ├── authMiddleware.js        # التحقق من JWT Token
│   └── roleMiddleware.js      # التحقق من الدور (user/agent/admin)
├── models/
│   ├── userModel.js             # نموذج المستخدم
│   ├── agentModel.js            # نموذج الوكيل
│   ├── adminModel.js            # نموذج الأدمن
│   ├── transactionModel.js      # نموذج المعاملة
│   └── notificationModel.js     # نموذج الإشعار
├── utils/
│   └── otpService.js            # إنشاء OTP + التحقق + التخزين في Redis
├── views/                       # واجهات الأدمن (HTML)
│   ├── login.html               # تسجيل دخول الأدمن
│   ├── dashboard.html           # لوحة المعلومات
│   ├── pending-users.html       # طلبات التسجيل (pending approvals) ← جديد
│   ├── users.html               # إدارة المستخدمين
│   ├── agents.html              # إدارة الوكلاء
│   ├── transactions.html        # سجل المعاملات
│   ├── user-details.html        # تفاصيل مستخدم + كل عملياته
│   └── notifications.html       # إرسال إشعارات + عرض الإشعارات
├── public/                      # CSS + JS للأدمن
│   ├── css/
│   └── js/
└── Laravel (php artisan serve)                    # الملف الرئيسي
```

### Flutter App (Mobile) - المبرمج الأول
```
lib/
├── main.dart
├── screens/
│   ├── login_screen.dart
│   ├── home_screen.dart
│   ├── transfer_screen.dart
│   ├── history_screen.dart
│   └── notifications_screen.dart  # شاشة الإشعارات (تشمل OTP)
├── services/
│   └── api_service.dart         # الربط بالـ Backend
└── models/
    ├── user.dart
    ├── transaction.dart
    └── notification.dart        # نموذج الإشعار
```

### Agent Web (HTML/CSS/JS) - المبرمج الأول
```
agent-web/
├── index.html                   # تسجيل دخول الوكيل
├── dashboard.html               # لوحة التحكم
├── deposit.html                 # صفحة الإيداع
├── withdraw.html                # صفحة السحب (خطوتين: طلب + OTP)
├── notifications.html           # صفحة الإشعارات
├── style.css
└── script.js
```

---

## 10. تقسيم الفريق (Final)

### المبرمج الأول: Flutter Developer
**المسؤوليات:**
- بناء Flutter App (المستخدم العادي) — **هذا فقط**
- إدارة HTTP Client (Dio) لإرسال Requests
- معالجة Responses وتحويل JSON لـ Objects
- تخزين JWT Token بشكل آمن
- معالجة Network Errors (Timeout, No Connection, 401, 500)
- **عرض الإشعارات**: شاشة إشعارات في التطبيق
- **لا يبني أي واجهة Web** (لا Agent Web ولا Admin Dashboard)
- **عرض OTP**: المستخدم يشوف كود التحقق في الإشعارات

**المفاهيم الشبكية المطبقة:**
- Request/Response Cycle
- HTTP Headers
- JWT Handling
- Network Error Handling
- Polling/Refreshing للإشعارات

### العضو الثاني: Backend + Admin + Notifications + OTP Developer (المبرمج الثاني)
**المسؤوليات:**
- بناء Backend API كامل (Laravel)
- تصميم قاعدة البيانات (PostgreSQL)
- بناء Admin Dashboard كامل (HTML/CSS/JS)
- بناء نظام الإشعارات (إنشاء، تخزين، إرسال، عرض)
- **بناء نظام OTP** (إنشاء رمز، تخزين في Redis، التحقق، انتهاء الصلاحية)
- تعريف Routes وEndpoints
- بناء Middleware للـ Authentication والـ Authorization
- الاتصال بقاعدة البيانات
- تنسيق Responses بـ JSON
- إدارة Ports والـ Server Configuration

**المفاهيم الشبكية المطبقة:**
- Routing
- Middleware Pipeline (Auth + Role Check)
- Port Binding
- TCP Connections
- Session Management
- Response Formatting
- Multi-Client Handling
- Notification System Architecture
- OTP Generation & Verification
- Redis Caching

---

## 11. واجهات الأدمن بالتفصيل الكامل

### 11.1 تسجيل دخول الأدمن
- يوزر + باسورد
- JWT Token يُرسل في كل طلب لاحق

### 11.2 لوحة المعلومات (Dashboard)
- عدد المستخدمين الكلي
- عدد الوكلاء الكلي
- **عدد طلبات التسجيل المعلقة (Pending)** ← badge أحمر
- إجمالي المعاملات اليوم
- إجمالي الرصيد في النظام
- عدد الإشعارات المرسلة اليوم
- عدد عمليات السحب بانتظار OTP

### 11.3 طلبات التسجيل (Pending Approvals) ← جديد
- جدول بكل الحسابات اللي تنتظر الموافقة (pending)
- يظهر: الاسم، رقم الجوال، البريد، تاريخ التسجيل
- **زر "موافقة" ✅**: يغير الحالة لـ active + يرسل إشعار للمستخدم
- **زر "رفض" ❌**: يغير الحالة لـ rejected + يرسل إشعار للمستخدم
- **بحث** برقم الجوال أو الاسم

### 11.4 إدارة المستخدمين (المفعلين)
- جدول بكل المستخدمين المفعلين (active) والموقفين (suspended)
- **بحث** عن مستخدم برقم الجوال أو الاسم
- **زر "تعليق الحساب" (Suspend)**: يغير حالة المستخدم لـ suspended — المستخدم ما يقدر يدخل أو يسوي أي عملية
- **زر "تفعيل الحساب" (Activate)**: يرجع الحالة لـ active — المستخدم يقدر يستخدم النظام
- **زر "تغذية حساب"**: يفتح نموذج يدخل فيه الأدمن المبلغ ويضغط "تأكيد" → يضاف الرصيد للمستخدم
- **زر "سحب من حساب"**: يفتح نموذج يدخل فيه الأدمن المبلغ ويضغط "تأكيد" → يخصم الرصيد من المستخدم
- **زر "إرسال إشعار"**: يفتح نموذج يكتب فيه الأدمن عنوان + نص الإشعار → يُرسل للمستخدم
- **زر "عرض التفاصيل"**: ينقل الأدمن لصفحة تفاصيل المستخدم

### 10.4 صفحة تفاصيل المستخدم (user-details.html)
**القسم الأول — بيانات المستخدم:**
- الاسم الكامل
- رقم الجوال
- البريد الإلكتروني
- الرصيد الحالي
- حالة الحساب (Active / Suspended)
- تاريخ التسجيل

**القسم الثاني — سجل العمليات المالية (جدول كامل):**

| # | التاريخ | النوع | المبلغ | العملة | **من** | **إلى** | الوصف | الحالة |
|---|---------|-------|--------|--------|--------|---------|-------|--------|
| 1 | 2026-08-20 | إيداع | 500 | SAR | الأدمن (Admin) | أحمد محمد | إيداع مباشر | تم |
| 2 | 2026-08-21 | تحويل | 200 | SAR | أحمد محمد | خالد عبدالله | تحويل | تم |
| 3 | 2026-08-21 | سحب | 100 | SAR | أحمد محمد | الوكيل (Agent) | سحب كاش | تم |

- **"من"**: يظهر اسم المرسل (مستخدم آخر / وكيل / أدمن / "نظام")
- **"إلى"**: يظهر اسم المستلم (مستخدم آخر / وكيل / أدمن / "نظام")
- **فلترة حسب**: تاريخ / نوع العملية / المبلغ / العملة

### 10.5 إدارة الوكلاء
- جدول بكل الوكلاء (اسم، رقم جوال، رصيد، حالة)
- **بحث** عن وكيل
- **زر "تعليق الحساب" (Suspend)**: يوقف الوكيل — ما يقدر يدخل أو يسوي عمليات
- **زر "تفعيل الحساب" (Activate)**: يرجع الوكيل للعمل
- **زر "إضافة وكيل جديد"**: نموذج يدخل فيه الأدمن بيانات الوكيل (اسم، رقم، باسورد)
- **زر "إرسال إشعار"**: يرسل إشعار لوكيل محدد

### 10.6 سجل المعاملات (System-wide)
- جدول بكل العمليات في النظام (من كل المستخدمين والوكلاء والأدمن)
- **فلترة حسب**: تاريخ / نوع / مستخدم / وكيل / أدمن / عملة
- **بحث** برقم العملية
- **الدخول لتفاصيل العملية**: يظهر نافذة/صفحة فيها:
  - رقم العملية
  - نوع العملية (إيداع/سحب/تحويل)
  - المبلغ والعملة
  - **من** (اسم المرسل + رقمه)
  - **إلى** (اسم المستلم + رقمه)
  - الوصف
  - التاريخ والوقت
  - الحالة (تم / بانتظار OTP / فشل)

### 10.7 إرسال الإشعارات (notifications.html)
**القسم الأول — إرسال إشعار جديد:**
- **اختيار المستلم**: قائمة منسدلة (مستخدم محدد / وكيل محدد)
- **بحث عن مستخدم/وكيل**: يكتب رقم الجوال ويختار
- **عنوان الإشعار**: حقل نصي
- **نص الإشعار**: textarea
- **زر "إرسال"**: يُرسل الإشعار ويُخزن في قاعدة البيانات

**القسم الثاني — سجل الإشعارات المرسلة:**
- جدول بكل الإشعارات (المستلم، العنوان، النص، التاريخ، حالة القراءة)

---

## 12. نظام الإشعارات بالتفصيل

### 12.1 أنواع الإشعارات

| النوع | الوصف | مثال |
|-------|-------|------|
| **transaction** | إشعار تلقائي بعد كل عملية مالية | "تم إيداع 500 ريال في حسابك" |
| **alert** | إشعار تحذيري من الأدمن | "حسابك على وشك التعليق، يرجى التواصل" |
| **message** | رسالة عامة من الأدمن | "تم تحديث سياسة الاستخدام" |
| **otp** | رمز تحقق لمرة واحدة | "كود التحقق: 123456" |

### 12.2 كيف يعمل؟

**عند حدوث عملية مالية (إيداع/سحب/تحويل):**
```
1. السيرفر ينفذ العملية
2. السيرفر ينشئ إشعار تلقائي في جدول notifications
3. المستخدم/الوكيل لما يفتح التطبيق/الموقع:
   - يرسل طلب GET /api/notifications
   - يستلم قائمة الإشعارات
   - يظهر عدد الإشعارات غير المقروءة (badge)
```

**عند إرسال إشعار من الأدمن:**
```
1. الأدمن يكتب عنوان + نص + يختار المستلم
2. يرسل POST /api/admin/notify-user (أو notify-agent)
3. السيرفر يخزن الإشعار في قاعدة البيانات
4. المستخدم/الوكيل يشوف الإشعار لما يفتح التطبيق/الموقع
```

### 12.3 عرض الإشعارات في التطبيق (Flutter)
- **أيقونة الجرس** في الشريط العلوي
- **عدد الإشعارات غير المقروءة** (badge أحمر)
- **شاشة الإشعارات**: قائمة بكل الإشعارات (الجديد فوق)
- **الضغط على إشعار**: يتحول لـ "مقروء" ويختفي الـ badge
- **إشعار OTP**: يظهر بارز بلون مختلف مع رقم كبير

### 12.4 عرض الإشعارات في واجهة الوكيل (Web)
- **صفحة الإشعارات** في القائمة الجانبية
- **جدول الإشعارات**: عنوان، نص، تاريخ، حالة القراءة
- **زر "تحديد كمقروء"**

---

## 13. نظام OTP للسحب (تفصيل كامل)

## 13.6 نظام موافقة الأدمن على التسجيل (Registration Approval)

### لماذا الموافقة؟
في الأنظمة المالية، لا يمكن السماح لأي شخص بالتسجيل واستخدام النظام مباشرة. يجب على الأدمن التحقق من هوية المستخدم وموافقته يدوياً.

### حالات الحساب (User Status)

| الحالة | الوصف | يقدر يستخدم النظام؟ |
|--------|-------|---------------------|
| **pending** | حساب جديد، ينتظر موافقة الأدمن | ❌ لا |
| **active** | تمت الموافقة، يستخدم النظام | ✅ نعم |
| **suspended** | موقف من الأدمن | ❌ لا |
| **rejected** | مرفوض من الأدمن | ❌ لا |

### خطوات التسجيل والموافقة

```
الخطوة 1: المستخدم يسجل حساب جديد
─────────────────────────────────
1. [USER] يفتح التطبيق ويضغط "إنشاء حساب"
2. [USER] يدخل البيانات:
   - الاسم الكامل
   - رقم الجوال
   - البريد الإلكتروني
   - كلمة المرور
3. [USER] يضغط "إنشاء حساب"
4. [SERVER] يستقبل POST /api/auth/register:
   - يتحقق من البيانات
   - يتحقق إن رقم الجوال مو مستخدم قبل
   - ينشئ الحساب بحالة "pending"
   - يرسل Response:
     { "success": true, "message": "تم إنشاء الحساب، ينتظر موافقة الإدارة" }
5. [USER] يشوف رسالة: "تم إنشاء حسابك بنجاح. حسابك قيد المراجعة، سيتم إشعارك بالموافقة."
6. [SERVER] ينشئ إشعار للأدمن: "طلب تسجيل جديد من أحمد محمد"

الخطوة 2: الأدمن يراجع ويوافق
─────────────────────────────
7. [ADMIN] يدخل لوحة التحكم
8. [ADMIN] يذهب لـ "طلبات التسجيل" (Pending Users)
9. [ADMIN] يشوف جدول بكل الحسابات المعلقة:
   - الاسم
   - رقم الجوال
   - البريد
   - تاريخ التسجيل
   - زر "موافقة" ✅
   - زر "رفض" ❌
10. [ADMIN] يضغط "موافقة" على حساب أحمد
11. [SERVER] يستقبل POST /api/admin/user/approve:
    - يتحقق من هوية الأدمن
    - يغير حالة المستخدم لـ "active"
    - يرسل إشعار للمستخدم: "تمت موافقة حسابك، يمكنك الآن استخدام المحفظة"
12. [USER] يستلم الإشعار في التطبيق
13. [USER] يقدر الآن:
    - يدخل بحسابه
    - يشوف رصيده
    - يحول
    - يطلب إيداع/سحب

الخطوة 3: الأدمن يرفض
─────────────────────
14. [ADMIN] يضغط "رفض" على حساب معين
15. [SERVER] يستقبل POST /api/admin/user/reject:
    - يتحقق من هوية الأدمن
    - يغير حالة المستخدم لـ "rejected"
    - يرسل إشعار للمستخدم: "تم رفض طلب التسجيل، تواصل مع الإدارة"
16. [USER] يستلم الإشعار
17. [USER] لما يحاول يدخل:
    - السيرفر يرفض الطلب
    - يظهر له: "تم رفض حسابك، تواصل مع الإدارة"
```

### التحقق عند تسجيل الدخول

```php
// app/Http/Controllers/AuthController.php
public function login(Request $request) {
    // ... التحقق من البيانات ...

    if ($user->status === 'pending') {
        return response()->json([
            'success' => false,
            'message' => 'حسابك قيد المراجعة، ينتظر موافقة الإدارة'
        ], 403);
    }

    if ($user->status === 'rejected') {
        return response()->json([
            'success' => false,
            'message' => 'تم رفض حسابك، تواصل مع الإدارة'
        ], 403);
    }

    if ($user->status === 'suspended') {
        return response()->json([
            'success' => false,
            'message' => 'حسابك موقف، تواصل مع الإدارة'
        ], 403);
    }

    // إذا active → يسمح بالدخول
    $token = JWTAuth::fromUser($user);
    return response()->json(['success' => true, 'token' => $token]);
}
```

### التحقق قبل أي عملية مالية

```php
// app/Http/Middleware/CheckUserStatus.php
public function handle(Request $request, Closure $next) {
    if ($request->user()->status !== 'active') {
        return response()->json([
            'success' => false,
            'message' => 'حسابك غير مفعل، لا يمكن إجراء العمليات'
        ], 403);
    }
    return $next($request);
}
```

### واجهة الأدمن — طلبات التسجيل (pending-users.html)

**جدول طلبات التسجيل:**

| # | الاسم | رقم الجوال | البريد | التاريخ | الحالة | الإجراء |
|---|-------|-----------|--------|---------|--------|---------|
| 1 | أحمد محمد | 0501234567 | ahmed@email.com | 2026-08-20 | ⏳ معلق | ✅ موافقة ❌ رفض |
| 2 | خالد عبدالله | 0507654321 | khaled@email.com | 2026-08-21 | ⏳ معلق | ✅ موافقة ❌ رفض |

- **زر "موافقة"**: يغير الحالة لـ active + يرسل إشعار للمستخدم
- **زر "رفض"**: يغير الحالة لـ rejected + يرسل إشعار للمستخدم
- **بحث**: برقم الجوال أو الاسم


### 13.1 لماذا OTP للسحب فقط؟
- **الإيداع**: الوكيل يعطي كاش للمستخدم → ما فيه خطر → **بدون OTP**
- **السحب**: الوكيل يأخذ كاش من المستخدم → **يحتاج تأكيد** → **مع OTP**

### 13.2 خطوات سحب كاش عبر الوكيل (مع OTP)

```
الخطوة 1: الوكيل يطلب السحب
─────────────────────────────
1. [AGENT] يفتح صفحة "سحب كاش"
2. [AGENT] يدخل:
   - رقم المستخدم: 0501234567
   - المبلغ: 100
   - العملة: SAR
3. [AGENT] يضغط "طلب سحب"
4. [SERVER] يستقبل الطلب:
   - يتحقق من هوية الوكيل (JWT)
   - يتحقق من رصيد المستخدم (يكفي؟)
   - ينشئ OTP (6 أرقام عشوائية)
   - يخزن OTP في Redis (مدة 5 دقائق)
   - يرسل إشعار للمستخدم: "كود التحقق للسحب: 123456"
5. [SERVER] يرسل Response:
   { "success": true, "message": "تم إرسال كود التحقق للمستخدم" }

الخطوة 2: المستخدم يستلم OTP
─────────────────────────────
6. [USER] يستلم إشعار في التطبيق:
   "طلب سحب 100 ريال"
   "كود التحقق: 123456"
   "اعطِ الكود للوكيل لتأكيد السحب"
7. [USER] يعطي الوكيل الكود شفهياً

الخطوة 3: الوكيل يؤكد السحب
───────────────────────────
8. [AGENT] يدخل الكود في الحقل المخصص
9. [AGENT] يضغط "تأكيد السحب"
10. [SERVER] يستقبل الطلب:
    - يتحقق من هوية الوكيل
    - يتحقق من الكود (مقابل Redis)
    - يتحقق من عدم انتهاء الصلاحية (5 دقائق)
    - يخصم 100 من المستخدم
    - يضيف 100 للوكيل
    - يسجل المعاملة
    - يحذف OTP من Redis (يستخدم مرة واحدة)
    - يرسل إشعار للمستخدم: "تم السحب بنجاح"
11. [SERVER] يرسل Response:
    { "success": true, "message": "تم السحب بنجاح" }
```

### 13.3 التحقق من OTP (في السيرفر)

```javascript
// التحقق من OTP
const verifyOTP = async (userId, code) => {
  // جلب OTP من Redis
  const storedCode = await redis.get(`otp:${userId}`);

  // التحقق من وجود الكود
  if (!storedCode) {
    return { success: false, message: "الكود منتهي الصلاحية أو غير موجود" };
  }

  // التحقق من تطابق الكود
  if (storedCode !== code) {
    return { success: false, message: "كود التحقق غير صحيح" };
  }

  // حذف الكود (يستخدم مرة واحدة)
  await redis.del(`otp:${userId}`);

  return { success: true, message: "الكود صحيح" };
};
```

### 13.4 إنشاء OTP (في السيرفر)

```javascript
// إنشاء OTP
const generateOTP = () => {
  return Math.floor(100000 + Math.random() * 900000).toString(); // 6 أرقام
};

// تخزين OTP في Redis
const storeOTP = async (userId, code) => {
  await redis.setex(`otp:${userId}`, 300, code); // 300 ثانية = 5 دقائق
};
```

### 13.5 صفحة سحب الوكيل (withdraw.html)

**المرحلة الأولى — طلب السحب:**
- حقل: رقم المستخدم
- حقل: المبلغ
- قائمة منسدلة: العملة (SAR / USD / إلخ)
- زر: "طلب سحب" → يرسل OTP للمستخدم

**المرحلة الثانية — تأكيد السحب:**
- رسالة: "تم إرسال كود التحقق للمستخدم"
- حقل: أدخل كود التحقق (6 أرقام)
- زر: "تأكيد السحب" → يرسل الكود للسيرفر للتحقق

---

## 14. سيناريو الاتصال (Communication Scenarios)

### 14.1 سيناريو 1: تسجيل الدخول (المستخدم)

```
1. [CLIENT] يرسل HTTP POST إلى http://server:3000/api/auth/login
   Body: { "phone": "0501234567", "password": "123456" }

2. [SERVER] يستقبل الطلب عبر Port 3000
   - يتحقق من البيانات
   - يبحث في قاعدة البيانات
   - يولد JWT Token

3. [SERVER] يرسل Response:
   Status: 200 OK
   Body: { "token": "eyJhbGciOiJIUzI1NiIs...", "user": {...} }

4. [CLIENT] يستلم Response
   - يخزن Token في Local Storage
   - يوجه المستخدم للشاشة الرئيسية

5. [CLIENT] في كل طلب لاحق:
   Header: Authorization: Bearer <token>
```

### 14.2 سيناريو 2: إيداع نقدي عبر الوكيل (بدون OTP)

```
1. [USER] يطلب إيداع 100 ريال من التطبيق
2. [USER] يروح للوكيل ويعطيه 100 ريال كاش
3. [AGENT] يفتح صفحة "إيداع" ويدخل:
   - رقم المستخدم: 0501234567
   - المبلغ: 100
   - العملة: SAR
   - يضغط "تأكيد الإيداع"
4. [SERVER] يستقبل الطلب:
   - يتحقق من هوية الوكيل (JWT)
   - يتحقق من رصيد الوكيل
   - يضيف 100 للمستخدم
   - يخصم 100 من الوكيل
   - يسجل المعاملة
   - ينشئ إشعار تلقائي: "تم إيداع 100 ريال في حسابك"
5. [USER] يفتح التطبيق ويشوف الإشعار
```

### 14.3 سيناريو 3: سحب نقدي عبر الوكيل (مع OTP) — خطوتين

```
الخطوة 1: طلب السحب
────────────────────
1. [USER] يطلب سحب 100 ريال من التطبيق
2. [USER] يروح للوكيل
3. [AGENT] يفتح صفحة "سحب كاش" ويدخل:
   - رقم المستخدم: 0501234567
   - المبلغ: 100
   - العملة: SAR
   - يضغط "طلب سحب"
4. [SERVER] يستقبل POST /api/agent/withdraw/request:
   - يتحقق من هوية الوكيل (JWT)
   - يتحقق من رصيد المستخدم (يكفي؟)
   - ينشئ OTP: 123456
   - يخزن OTP في Redis (5 دقائق)
   - يرسل إشعار للمستخدم: "كود التحقق: 123456"
5. [SERVER] يرسل Response:
   { "success": true, "message": "تم إرسال كود التحقق" }

الخطوة 2: تأكيد السحب
─────────────────────
6. [USER] يستلم إشعار: "كود التحقق للسحب: 123456"
7. [USER] يعطي الوكيل الكود: "123456"
8. [AGENT] يدخل الكود ويضغط "تأكيد السحب"
9. [SERVER] يستقبل POST /api/agent/withdraw/verify:
   - يتحقق من هوية الوكيل
   - يتحقق من الكود (مقابل Redis)
   - يتحقق من عدم انتهاء الصلاحية
   - يخصم 100 من المستخدم
   - يضيف 100 للوكيل
   - يسجل المعاملة
   - يحذف OTP من Redis
   - يرسل إشعار: "تم السحب بنجاح"
10. [SERVER] يرسل Response:
    { "success": true, "message": "تم السحب بنجاح" }
11. [USER] يستلم إشعار: "تم سحب 100 ريال من حسابك"
```

### 14.4 سيناريو 4: الأدمن يغذي حساب مستخدم

```
1. [ADMIN] يدخل لوحة التحكم
2. [ADMIN] يذهب لـ "إدارة المستخدمين"
3. [ADMIN] يبحث عن المستخدم برقم الجوال
4. [ADMIN] يضغط "تغذية حساب"
5. [ADMIN] يدخل المبلغ (مثلاً 500 ريال) ويضغط "تأكيد"
6. [SERVER] يستقبل الطلب:
   - يتحقق من هوية الأدمن (JWT + Role Check)
   - يضيف 500 للمستخدم
   - يسجل المعاملة (admin_id = id الأدمن)
   - ينشئ إشعار تلقائي: "تم إيداع 500 ريال في حسابك"
7. [ADMIN] يشوف رسالة "تم الإيداع بنجاح"
8. [USER] يستلم الإشعار في التطبيق
```

### 14.5 سيناريو 5: الأدمن يشوف تفاصيل مستخدم وعملياته

```
1. [ADMIN] يدخل "إدارة المستخدمين"
2. [ADMIN] يضغط على اسم/رقم مستخدم
3. [SERVER] يرسل:
   - بيانات المستخدم (اسم، رقم، رصيد، حالة)
   - قائمة كل عملياته المالية مع أعمدة:
     - التاريخ
     - النوع (إيداع/سحب/تحويل)
     - المبلغ والعملة
     - "من" (اسم المرسل)
     - "إلى" (اسم المستلم)
     - الوصف
     - الحالة
4. [ADMIN] يشوف كل شيء في صفحة واحدة منظمة
```

### 14.6 سيناريو 6: الأدمن يعلق حساب وكيل

```
1. [ADMIN] يدخل "إدارة الوكلاء"
2. [ADMIN] يبحث عن الوكيل
3. [ADMIN] يضغط "تعليق الحساب"
4. [SERVER] يستقبل الطلب:
   - يتحقق من هوية الأدمن
   - يغير حالة الوكيل لـ "suspended"
   - ينشئ إشعار للوكيل: "تم تعليق حسابك، تواصل مع الإدارة"
5. [AGENT] لما يحاول يدخل بعدها:
   - السيرفر يرفض الطلب
   - يظهر له "حسابك موقف، تواصل مع الإدارة"
6. [AGENT] يشوف الإشعار في صفحة الإشعارات
```

### 14.7 سيناريو 7: الأدمن يرسل إشعار لعميل

```
1. [ADMIN] يدخل "إرسال الإشعارات"
2. [ADMIN] يختار نوع المستلم: "مستخدم"
3. [ADMIN] يبحث برقم الجوال ويختار المستخدم
4. [ADMIN] يكتب:
   - العنوان: "تحديث النظام"
   - النص: "سيتم تحديث النظام الليلة الساعة 12، يرجى عدم إجراء عمليات"
5. [ADMIN] يضغط "إرسال"
6. [SERVER] يستقبل الطلب:
   - يتحقق من هوية الأدمن
   - يخزن الإشعار في جدول notifications
7. [USER] يفتح التطبيق:
   - يرسل GET /api/notifications
   - يستلم الإشعار
   - يظهر badge أحمر على أيقونة الجرس
8. [USER] يضغط على الإشعار → يتحول لـ "مقروء"
```

---

## 15. الجدول الزمني

| الأسبوع | العميل (المبرمج الأول) | الخادم + الأدمن (المبرمج الثاني) |
|---------|----------------|----------------------|
| 1 | تصميم UI/UX للتطبيق + تصميم صفحات الوكيل | تصميم قاعدة البيانات + هيكل الملفات |
| 2 | بناء شاشات التسجيل والدخول | بناء Auth API (مستخدم + وكيل + أدمن) |
| 3 | ربط Auth بالـ API + بناء صفحة دخول الوكيل | بناء JWT Middleware + Role Middleware |
| 4 | بناء شاشة الرصيد والتحويل | بناء Wallet API (Balance + Transfer) |
| 5 | بناء صفحة إيداع الوكيل + صفحة سحب الوكيل (خطوتين) | بناء Agent Controller (Deposit + Withdraw مع OTP) |
| 6 | بناء شاشة الإشعارات (Flutter) + صفحة إشعارات الوكيل | بناء Notification Controller + OTP Service + Redis |
| 7 | معالجة Network Errors | بناء Admin Dashboard (HTML/CSS) |
| 8 | اختبار التطبيق والوكيل | بناء Admin Controller (طلبات التسجيل + موافقة/رفض + إحصائيات) |
| 9 | إصلاح الأخطاء | بناء Admin Controller (إيداع/سحب + تفاصيل + إرسال إشعارات) |
| 10 | إعداد العرض النهائي | اختبار شامل + إعداد العرض النهائي |

**إجمالي المدة: 10 أسابيع**

---

## 16. المخرجات المتوقعة (Deliverables)

1. **تطبيق عميل** (Flutter) - للمستخدم العادي مع شاشة الإشعارات (تشمل OTP)
2. **واجهة وكيل** (Web) - للإيداع/السحب النقدي مع نظام OTP خطوتين
3. **لوحة تحكم الأدمن** (Web) - لإدارة النظام كامل مع إرسال الإشعارات
4. **خادم** (Laravel) - يستقبل ويعالج Requests ويدير الإشعارات ونظام OTP
5. **توثيق API** (Swagger) - يوضح Endpoints والـ Data Flow
6. **مخطط الشبكة** - يوضح Client-Server Architecture
7. **عرض تقديمي** - يشرح المفاهيم الشبكية المطبقة

---

## 17. المخاطر والتحديات

| المخاطر | التأثير | الحل |
|---------|---------|------|
| تعقيد معالجة المعاملات المالية | عالٍ | استخدام Database Transactions (ACID) |
| مشاكل الأمان | عالٍ | تشفير البيانات، JWT، OTP، Validation، Role Check |
| صعوبة التواصل بين الفريق | متوسط | اجتماعات يومية، Swagger API Docs |
| ضغط الوقت | متوسط | التركيز على MVP فقط |
| كثرة الإشعارات | منخفض | Pagination (عرض 10 إشعارات في الصفحة) |
| انتهاء صلاحية OTP | منخفض | Redis TTL (5 دقائق) + رسالة واضحة للمستخدم |

---

## 18. الخلاصة

هذا المشروع يطبق **مبدأ Client-Server Architecture** بشكل عملي من خلال:
- **ثلاثة عملاء** (Flutter App + Agent Web + Admin Dashboard) يرسلون Requests
- **خادم واحد** (Laravel API) يعالج ويستجيب
- **بروتوكول HTTP** كوسيلة الاتصال
- **JSON** كـ Data Format
- **JWT** كآلية توثيق عبر الشبكة (طبقة أولى)
- **OTP** كآلية تحقق إضافية للسحب (طبقة ثانية)
- **TCP/IP** كبنية تحتية للنقل
- **Role-Based Access Control** لفصل الصلاحيات
- **نظام إشعارات** يربط بين الأطراف ويوفر تواصل فعال
- **Redis** لتخزين OTP مؤقتاً

مع التركيز على **الأمان متعدد الطبقات**، **الموثوقية**، و**معالجة الأخطاء** على مستوى الشبكة.

---

*تم إعداد هذا المقترح بواسطة:*
- [اسم العضو الأول] - Client-Side Developer (Flutter + Web)
- [اسم العضو الثاني] - Backend + Admin + Notifications + OTP Developer

*التاريخ: أغسطس 2026*
